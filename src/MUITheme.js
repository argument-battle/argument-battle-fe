import { createMuiTheme } from '@material-ui/core/styles';

const MUITheme = createMuiTheme({
    palette: {
        background: { paper: '#CBDCD7', default: '#e6efeb' }
    }
});

export { MUITheme };
