import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
    card: {
        overflow: 'unset'
    },
    avatar: {
        flexShrink: 1,
        maxWidth: '30%'
    },
    root: {
        flexWrap: 'wrap'
    },
    action: {
        position: 'absolute',
        right: '2%'
    }
});
