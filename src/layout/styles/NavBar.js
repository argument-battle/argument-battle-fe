import { makeStyles } from '@material-ui/core/styles';

const drawerWidth = '25%';

export default makeStyles({
    drawerPaper: {
        width: drawerWidth
    },
    drawer: {
        width: drawerWidth
    }
});
